// these are if in production where ffmpeg is not included in the environmet variables
// path = "path to environment's location of ffmpeg";
// Ffmpeg.setFfmpegPath(path) 
// Ffmpeg.setFfprobePath(path) 
// Ffmpeg.setFlvtoolPath(path) 